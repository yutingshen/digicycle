/**
 * Flowtype definitions for TokenId
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.11.0
 */

import { TokenID } from "../generated/BasicTypes_pb";

/**
 * Normalized token ID returned by various methods in the SDK.
 */
declare export class TokenId {
  shard: number;
  realm: number;
  token: number;
  constructor(shard: number, realm: number, token: number): this;
  constructor(tokenId: TokenIdLike): this;
  static fromString(id: string): TokenId;
  static _fromProto(tokenId: TokenID): TokenId;
  toString(): string;
  static fromSolidityAddress(address: string): TokenId;
  toSolidityAddress(): string;
  _toProto(): TokenID;
}
/**
 * Input type for an ID of an token on the network.
 *
 * In any form, `shard` and `realm` are assumed to be 0 if not provided.
 *
 * Strings may take the form `'<shard>.<realm>.<token>'` or `'<token>'`.
 *
 * A bare `number` will be taken as the token number with shard and realm of 0.
 */
export type TokenIdLike =
  | {
      shard?: number,
      realm?: number,
      token: number,
      ...
    }
  | string
  | number
  | TokenId;
